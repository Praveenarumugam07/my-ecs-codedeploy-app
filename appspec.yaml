version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "<TASK_DEFINITION_PLACEHOLDER>"
        LoadBalancerInfo:
          ContainerName: "myapp-container"
          ContainerPort: 80
